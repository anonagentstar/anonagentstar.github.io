# AgentSTAR: anonymous ICLR submission

## Checking the page

```
python scripts/serve.py 8765          # in this directory, then open http://127.0.0.1:8765/ (plain http.server cannot seek the timelapse video)
python scripts/smoke_test.py          # headless check of every carousel; needs: pip install playwright && playwright install chromium
```
